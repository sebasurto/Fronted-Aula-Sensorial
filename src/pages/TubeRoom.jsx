import React from 'react';
import { View, Text, Button } from 'react-native';
import styles from '../styles/styles';
function TubeRoom({ navigation }) {
  return (
    <View>
      <Text style={styles.title}> Cuarto de tubos</Text>
    </View>
  );
}

export default TubeRoom;